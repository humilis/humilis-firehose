---
firehose:
    description:
        A dummy environment used for testing the firehose layer.

    layers:
        - layer: storage

        - layer: firehose-resource
          layer_type: firehose-resource

        - layer: firehose
          layer_type: firehose
          firehose_resource_layer: firehose-resource
          secrets_namespace: "humilis:firehose:redshift"
          streams:
              - name: DeliveryStream1
                prefix: str1/
                table: "_test.stream1"
              - name: DeliveryStream2
                prefix: str2/
                table: "_test.stream2"
